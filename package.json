{
  "name": "ligatures",
  "version": "0.0.0-0",
  "description": "Get a string array of ligatures for a given font",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf",
    "start": "yarn run build -- --watch --preserveWatchOutput"
  },
  "author": "Peter Mikitsh",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "@types/fontkit": "^1.8.0",
    "fontkit": "^1.8.1",
    "tslib": "^2.0.0"
  }
}
